version: '3.8'

services:
  mcp-gatk-HaplotypeCaller:
    build: .
    image: mcp-gatk-HaplotypeCaller:latest
    container_name: mcp-gatk-HaplotypeCaller
    ports:
      - "8000:8000"
    environment:
      - MCP_SERVER_NAME=gatk-HaplotypeCaller
    volumes:
      - ./workspace:/app/workspace
      - ./output:/app/output
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    
