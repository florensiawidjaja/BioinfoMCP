version: '3.8'

services:
  mcp-deeptools-correctGCBias:
    build: .
    image: mcp-deeptools-correctGCBias:latest
    container_name: mcp-deeptools-correctGCBias
    ports:
      - "8000:8000"
    environment:
      - MCP_SERVER_NAME=deeptools-correctGCBias
    volumes:
      - ./workspace:/app/workspace
      - ./output:/app/output
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    
