version: '3.8'

services:
  mcp-multiqc:
    build: .
    image: mcp-multiqc:latest
    container_name: mcp-multiqc
    ports:
      - "8000:8000"
    environment:
      - MCP_SERVER_NAME=multiqc
    volumes:
      - ./workspace:/app/workspace
      - ./output:/app/output
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    
