version: '3.8'

services:
  mcp-bwa:
    build: .
    image: mcp-bwa:latest
    container_name: mcp-bwa
    ports:
      - "8000:8000"
    environment:
      - MCP_SERVER_NAME=bwa
    volumes:
      - ./data:/data
      - ./config:/config
    restart: unless-stopped

    