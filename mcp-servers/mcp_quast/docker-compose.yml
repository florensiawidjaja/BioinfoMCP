version: '3.8'

services:
  mcp-quast:
    build: .
    image: mcp-quast:latest
    container_name: mcp-quast
    ports:
      - "8000:8000"
    environment:
      - MCP_SERVER_NAME=quast
    volumes:
      - ./workspace:/app/workspace
      - ./output:/app/output
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    
