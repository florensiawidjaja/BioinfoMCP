version: '3.8'

services:
  mcp-gatk-SelectVariants:
    build: .
    image: mcp-gatk-SelectVariants:latest
    container_name: mcp-gatk-SelectVariants
    ports:
      - "8000:8000"
    environment:
      - MCP_SERVER_NAME=gatk-SelectVariants
    volumes:
      - ./workspace:/app/workspace
      - ./output:/app/output
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    
